{% extends 'base.html.twig' %}

{% block title %}Dirty Cars API{% endblock %}

{% block body %}
<div class="mx-auto max-w-3xl w-11/12 text-lg flex items-center justify-center min-h-screen">
    <div class="header">
        <img src="/images/car.svg" alt="Dirty Cars Logo">
    <h1 class="text-center py-4">
    <a href="{{ path('app_register') }}" class="bg-transparent hover:bg-blue-700 mt-5 text-dark font-bold py-3 px-8 rounded-xl shadow-xl transform hover:scale-105 transition-transform duration-300">
        S'inscrire
    </a>
        <a href="{{ path('api_doc') }}" class="text-blue-500 hover:text-blue-700">Accéder à l'API Swagger</a>
</h1>
    </div>
</div>
{% endblock %}
